<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- 主 App 主题 -->
    <style name="Theme.Game4399" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorPrimaryVariant">@color/brand_primary_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/brand_accent</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="android:windowBackground">@color/bg_surface</item>
        <item name="android:statusBarColor" tools:targetApi="21">@color/brand_primary_dark</item>
        <item name="android:navigationBarColor">@color/bg_surface</item>
        <!-- 刘海屏适配 -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="30">shortEdges</item>
    </style>

    <!-- 游戏界面主题：沉浸式（用 WindowInsetsControllerCompat 隐藏状态栏，不用 windowFullscreen） -->
    <!-- 注：windowFullscreen=true 会设置 FLAG_FULLSCREEN，在 WebView 中阻止软键盘弹出 -->
    <style name="Theme.Game4399.Game" parent="Theme.Game4399">
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@android:color/black</item>
        <!-- 刘海屏适配：内容延伸到刘海区域 -->
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="30">shortEdges</item>
    </style>

    <!-- 文字样式 -->
    <style name="TextAppearance.Headline" parent="TextAppearance.Material3.HeadlineSmall">
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.Title" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:textColor">@color/text_primary</item>
    </style>

    <style name="TextAppearance.Body" parent="TextAppearance.Material3.BodyMedium">
        <item name="android:textColor">@color/text_secondary</item>
    </style>

    <!-- 游戏卡片 -->
    <style name="Widget.GameCard" parent="">
        <item name="android:background">@drawable/bg_game_card</item>
        <item name="android:padding">@dimen/spacing_md</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?attr/selectableItemBackground</item>
    </style>

    <!-- 手柄按钮 -->
    <style name="Widget.GamepadButton" parent="">
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:foreground">?attr/selectableItemBackgroundBorderless</item>
    </style>

</resources>
